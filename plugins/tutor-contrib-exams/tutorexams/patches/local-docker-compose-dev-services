exams:
  environment:
    DJANGO_SETTINGS_MODULE: edx_exams.settings.tutor.development
  command: ./manage.py runserver 0.0.0.0:8740
  stdin_open: true
  tty: true
  ports:
    - "8740:8740"
  networks:
    default:
      aliases:
        - "{{ EXAMS_HOST }}"
  # warning, volumes cannot be empty so this will fail if there are no mounts
  volumes:
    {%- for mount in iter_mounts(MOUNTS, "exams") %}
      - {{ mount }}
    {%- endfor %}
