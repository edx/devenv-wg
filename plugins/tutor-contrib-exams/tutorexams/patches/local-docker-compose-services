exams:
  image: {{ EXAMS_DOCKER_IMAGE }}
  restart: unless-stopped
  volumes:
    - ../plugins/exams/apps/settings/tutor:/openedx/exams/edx_exams/settings/tutor:ro
  depends_on:
    - lms
    {% if RUN_MYSQL %}- mysql{% endif %}
